<%= form_with(model: ledger_expense, local: true) do |form| %>
  <% if ledger_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ledger_expense.errors.count, "error") %> prohibited this ledger_expense from being saved:</h2>

      <ul>
      <% ledger_expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :ledger_id %>
    <%= form.text_field :ledger_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="field">
    <%= form.label :payment_notes %>
    <%= form.text_area :payment_notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
