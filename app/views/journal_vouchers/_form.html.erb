<%= form_with(model: journal_voucher, local: true) do |form| %>
  <% if journal_voucher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(journal_voucher.errors.count, "error") %> prohibited this journal_voucher from being saved:</h2>

      <ul>
      <% journal_voucher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
